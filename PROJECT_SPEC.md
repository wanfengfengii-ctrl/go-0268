# 名优绿茶鲜叶批入杀青机前联合检验闭环服务

## 项目目标

建设一个以鲜叶批号和筐封签为核心聚合边界的 Go HTTP 后端。任务从待锁定开始，原子固定茶园小区、采摘轮次、规则摘要、资源、阈值、人员名单和任务代次；随后完成双人收青、摊青覆盖采集、嫩度守恒核验、农残快筛、理化复测、异常复判及两人独立复核，最终通过单写屏障进入可杀青、风险隔离或已取消。服务使用事务型持久化、逻辑时钟、带期限的资源租约、操作号幂等记录、不可覆盖证据版本链和启动恢复扫描，确保并发竞争、设备故障、进程重启及迟到数据下仍可确定裁定。生产实现规划为约 20—28 个 Go 文件、2200—2800 行有效生产代码，分布于至少 4 个业务包；规模来自聚合状态机、账簿、定点算术、持久化、并发裁定、设备调用和 HTTP 契约。

## 端到端业务流程

1. 目录管理员登记虚构或真实茶园小区、允许的采摘轮次、制茶规则版本、阈值尺度、摊青时点模板、资源清单和合格人员；规则发布后生成稳定摘要，锁定任务只能引用仍为当前版本且与茶园、轮次匹配的摘要。
2. 创建待锁定任务后，一次事务固定鲜叶批号、全部筐封签、摊青槽位、鼓风支路、探头窗口、盲码、嫩度点位、快筛板孔、杀青机时隙、阈值、复核名单和任务代次；同时获取所有租约。任一标识重复、资源冲突或摘要陈旧时整体回滚。
3. 两名不同且合格的收青人员以操作号确认同一批次和任务代次；确认闭合后进入槽位占用中，再原子启动摊青采集。相同操作号与相同规范化内容重试返回原结果，不同内容返回冲突。
4. 按锁定时点与筐集合提交环境湿度、含水率、叶温、失水量、红变和补测信息，形成不可部分写入的覆盖矩阵；随后提交各嫩度点位的单芽、一芽一叶、老叶、红变叶及样本总数，守恒通过后才能推进。
5. 封存三联样本后才允许揭示盲码并占用指定快筛板孔；读取农残抑制率并核验板孔、盲码和代次，继而由水分仪完成叶温与含水率复测。设备拒绝、断连、超时或格式错误只追加可审计调用尝试，并按逻辑时间重试。
6. 失水不足、叶温越界、红变比例异常、农残疑阳或三联样本分歧触发当前代次唯一复判，明确覆盖受影响筐、盲码、时点和孔位；复判闭合后进入待独立复核。两名与收青人员不重合的合格复核人员分别签署，之后可杀青、风险隔离和取消请求竞争同一终局屏障；杀青凭据只能生成一次，完成实际启动确认后状态由可杀青变为已杀青。

## 核心组件与职责

1. 茶园与制茶规则目录：维护茶园小区、采摘轮次、规则版本与摘要、阈值尺度、摊青时点、资源能力及人员资格，并提供锁定期一致性校验。
2. 鲜叶入杀青任务聚合：实现十二个业务状态、任务代次、双人收青、操作幂等、状态迁移、复核资格和终局单写屏障。
3. 筐样本与资源占用账簿：管理鲜叶批号、筐封签和盲码的一次性门闩，以及槽位、鼓风支路、杀青机时隙和板孔租约；负责换槽、续租、释放及重启恢复。
4. 摊青失水及嫩度采集账簿：保存按筐和时点构成的覆盖格、定点物理量、嫩度守恒结果与设备尝试，禁止覆盖有效证据。
5. 农残复判及终局仲裁器：维护样本封存、揭盲、农残与理化证据版本链、异常复判代次、独立复核和唯一杀青凭据或其他终局。
6. Go HTTP API：提供 JSON 接口、中间件、事务边界、稳定错误响应、确定排序、健康检查和启动恢复入口，不包含独立前端。

## 领域规则与不变量

1. 状态严格为待锁定、待收青确认、槽位占用中、摊青采集中、嫩度核验中、农残快筛中、理化复测中、待独立复核、可杀青、已杀青、风险隔离、已取消；只有规则表定义的迁移合法，风险隔离和已取消为终态，已杀青为完成终态。
2. 鲜叶批号、筐封签和盲码在开放任务中全局唯一；盲码在样本封存前不得揭示。槽位、鼓风支路、杀青机时隙及快筛板孔以区间租约排他占用，批量申请按资源种类和标识排序，冲突时不得留下部分租约。
3. 每个写操作携带任务代次、操作号和规范化请求摘要。相同操作号与相同摘要幂等返回首次结果；相同操作号内容不同报 OPERATION_CONTENT_CONFLICT；陈旧代次报 STALE_TASK_GENERATION。
4. 覆盖矩阵以锁定的摊青时点×筐封签为全集；同一格禁止重复有效提交。嫩度分类计数必须非负且总和等于样本总数。缺格、重复格或守恒失败均不得推进，也不得把非法值写入有效账簿。
5. 含水率、失水率和农残抑制率按万分比整数保存，叶温和环境湿度按规定小数位整数保存，样本质量使用固定质量单位；解析与派生计算采用检查型整数运算，拒绝超长、非法符号、除零、乘加溢出和舍入尺度不匹配。边界统一采用规则中显式的闭区间、开区间标志，不允许调用方自行解释。
6. 设备调用使用确定调用键、尝试序号、逻辑时间和脚本化结果。失败尝试不得生成合格读数、释放资源或改变业务阶段；格式正确且代次匹配的成功结果才可追加证据。旧复判代次及终态后的迟到结果保留原始调用审计，但不得写入当前证据链。
7. 异常复判对同一任务当前代次只能存在一个活动版本；新复判代次必须引用前一版本并完整声明受影响的筐、盲码、时点和孔位。农残、嫩度、理化及复判证据只追加不覆盖，结论读取仅采用当前有效代次。
8. 收青必须由两名不同合格人员确认；独立复核也必须由两名不同合格人员完成，且任何复核人不得是本任务收青人。全部证据闭合且阈值满足后方可生成唯一杀青凭据；杀青、隔离、取消通过同一持久化比较交换竞争，终态后普通操作统一拒绝。

## 数据模型与持久化

1. GardenPlot、PickingRound、ProcessingRuleVersion：保存茶园小区、采摘轮次、适用关系、定点尺度、阈值边界、时点模板、规则摘要和发布状态。
2. LeafIntakeTask：包含任务 ID、鲜叶批号、茶园小区、采摘轮次、规则摘要、任务代次、当前状态、锁定快照、版本号、逻辑时间及唯一终局字段。
3. BasketSample、BlindSample、TendernessPoint：关联筐封签、盲码承诺与揭示状态、三联样本、嫩度点位和受影响范围；唯一索引约束开放任务中的批号、封签及盲码。
4. ResourceLease：记录资源类型、资源标识、占用区间、任务、代次、租约版本和释放原因；数据库唯一约束配合事务检查防止重叠占用，并由恢复器重建未终结任务的有效租约视图。
5. WitheringCell、TendernessEvidence、AssayEvidence、RetestEvidence：分别保存时点覆盖格、嫩度守恒计数、农残板孔读数和含水率/叶温复测；均带证据版本、操作号、采集来源和不可变校验摘要。
6. DeviceAttempt、RejudgmentCase、ReviewDecision、TerminalDecision、IdempotencyRecord：保存设备尝试序列、复判代次链、人员独立结论、终局比较交换结果及操作号请求/响应摘要。持久化采用 SQLite 事务和迁移文件，启用外键、唯一索引及 WAL；所有成功写操作与审计事件同事务提交，重启后从数据库恢复，不依赖内存状态。

## 公开接口

1. POST /api/v1/tasks 创建任务；POST /api/v1/tasks/{id}/lock 原子锁定快照与资源；GET /api/v1/tasks/{id} 返回状态、覆盖进度、当前代次和资源摘要。
2. POST /api/v1/tasks/{id}/receipts 提交双人收青；POST /api/v1/tasks/{id}/withering/start 启动或换槽；POST /api/v1/tasks/{id}/withering/readings 批量提交摊青覆盖格。
3. POST /api/v1/tasks/{id}/tenderness 提交嫩度计数；POST /api/v1/tasks/{id}/samples/seal 封存样本；POST /api/v1/tasks/{id}/samples/reveal 揭盲。
4. POST /api/v1/tasks/{id}/assays/start 占用板孔；POST /api/v1/tasks/{id}/assays/read、POST /api/v1/tasks/{id}/retests/read 接收设备结果；POST /api/v1/device-attempts/{id}/retry 按逻辑时钟重试。
5. POST /api/v1/tasks/{id}/rejudgments 创建或提交当前代次复判；POST /api/v1/tasks/{id}/reviews 提交独立复核。
6. POST /api/v1/tasks/{id}/terminal 接受 release、isolate 或 cancel 命令并竞争单写屏障；POST /api/v1/tasks/{id}/fixation/confirm 将唯一可杀青凭据确认成已杀青。所有拒绝返回包含稳定 code、operation_id、task_generation 和排序后 reasons 的 JSON；原因键依次按茶园小区、鲜叶批号、筐封签、摊青时点、检测孔位排序。

## 失败边界

1. 锁定、换槽、板孔启动和终局裁定使用单个数据库事务及聚合版本条件更新；任何校验、唯一约束或提交失败都回滚业务行、租约、幂等响应和审计事件。
2. 批量覆盖、嫩度、揭盲、农残和复判提交先完成全量解析、算术及代次校验，再一次提交；失败不得留下部分覆盖格、盲码揭示、复判证据或派生结论。
3. 设备适配器与业务事务隔离：先持久化待调用尝试，外部调用完成后以调用键和代次追加结果；超时或进程崩溃可在重启后确定恢复，不把未知结果当作成功。
4. 租约不依据墙钟静默释放。恢复器扫描开放任务、未决设备尝试和终局记录，以逻辑时间恢复可重试项；只有显式终局或经规则允许的原子换槽才能释放相应占用。
5. 错误码至少覆盖 PLOT_ROUND_MISMATCH、STALE_RULE_DIGEST、DUPLICATE_BASKET_SEAL、BLIND_CODE_CONFLICT、BLIND_CODE_EARLY_REVEAL、RESOURCE_LEASE_CONFLICT、COVERAGE_MISSING、COVERAGE_DUPLICATE、COUNT_NOT_CONSERVED、FIXED_POINT_OVERFLOW、THRESHOLD_BOUNDARY_CONFLICT、DEVICE_RETRYABLE、REJUDGMENT_GENERATION_CONFLICT、ROLE_OVERLAP、OPERATION_CONTENT_CONFLICT、STALE_TASK_GENERATION、TERMINAL_STATE_REJECTED；多原因响应按统一比较器稳定排序。
6. HTTP 请求取消、JSON 格式错误和数据库忙不会伪造业务成功。可重试数据库冲突有固定次数上限并映射稳定错误；已提交的幂等结果可在客户端超时后安全重取。

## 验收标准

1. 锁定成功后能读取完整且不可变的茶园、轮次、批号、封签、资源、探头窗口、盲码、嫩度点位、板孔、时隙、全部阈值和任务代次快照；错配或陈旧摘要使事务完全回滚。
2. 对批号、封签、盲码、槽位、鼓风支路、杀青时隙和板孔发起同步并发竞争时恰有一个任务成功；并发换槽或启动也不产生重复占用或部分租约。
3. 收青、摊青、封存、揭盲、复判和复核均验证当前代次及操作号；同内容重试返回完全相同结果，内容冲突、错误茶园、角色重合和终态操作返回稳定错误码及确定排序原因。
4. 只有覆盖全部锁定时点与筐位、嫩度计数守恒且所有定点值解析和计算安全时才推进；阈值开闭边界、缺失、重复、负数、除零和溢出均有精确断言且失败不写有效证据。
5. 可控探头、快筛读板器和水分仪脚本能够确定重现拒绝、断连、超时、格式错误与随后成功；每次尝试的对象、序号和逻辑时间可审计，重启后继续同一尝试序列且租约仍有效。
6. 异常仅创建一个当前复判代次并覆盖规定筐位、盲码、时点和孔位；旧代次迟到读数仍可审计但不覆盖证据、不改变当前结论。
7. 全部证据闭合后，两名不同且与收青角色不重合的合格人员完成复核；并发放行、隔离和取消恰有一个终局，放行只生成一个杀青凭据，终态后数据不再改变聚合。
8. 仓库包含 go.mod、数据库迁移、可运行服务入口和支持 linux/amd64、linux/arm64 构建的 Dockerfile；生产代码保持至少 20 个 Go 文件、至少 4 个有业务职责的包及约 2000—3000 行有效代码。公开测试分布于至少 4 个测试文件且不少于 12 个用例，并预留从同一初始 main 建立的 10 个独立缺陷分支所需的状态机、租约、算术、幂等、恢复和终局测试定位点。

## 确定性测试场景

1. 锁定完整快照成功，并验证规则摘要和所有资源、阈值、人员、代次不可变。
2. 茶园小区与采摘轮次错配、规则摘要陈旧分别失败且数据库无租约残留。
3. 两个任务经同步屏障并发抢占相同批号、封签、槽位、鼓风支路、时隙或板孔，仅一个提交成功。
4. 同一操作号同内容重复收青返回相同响应；改变人员或内容后返回 OPERATION_CONTENT_CONFLICT。
5. 样本封存前揭盲被拒绝；重复盲码和不同任务揭示不一致被确定拦截。
6. 摊青矩阵缺失一个时点、重复同一格、包含未锁定筐时分别失败；完整矩阵原子成功。
7. 单芽、一芽一叶、老叶、红变叶计数之和不等于样本总数时拒绝，原有效证据数量保持不变。
8. 含水率、叶温、湿度、失水率、样本质量和农残抑制率覆盖合法边界、边界外值、除零、超长与整数溢出。
9. 探头、读板器和水分仪按固定脚本经历拒绝、断连、超时、格式错误及成功，验证尝试序号和逻辑时间。
10. 服务在资源锁定和设备待重试状态下重启，恢复后继续任务且另一任务仍无法抢占资源。
11. 异常三联样本创建复判代次；旧代次结果晚到后仅进入审计，当前结论和证据摘要不变。
12. 收青人与复核人重合、两次复核为同一人、陈旧任务代次分别被拒绝。
13. 同步并发提交杀青放行、风险隔离和取消，断言唯一终局、唯一凭据或唯一隔离/取消记录。
14. 进入已杀青、风险隔离或已取消后提交读数、复判、复核、换槽及普通操作，均返回 TERMINAL_STATE_REJECTED 且持久化状态未变化。

## 组件追踪关系

1. 茶园与制茶规则目录对应锁定快照、茶园/轮次适配、摘要陈旧检查、阈值边界和人员资格，主要覆盖验收 1、3、4、7。
2. 鲜叶入杀青任务聚合对应十二状态迁移、任务代次、双人收青、幂等记录和终局屏障，主要覆盖验收 1、3、7。
3. 筐样本与资源占用账簿对应批号/封签/盲码唯一门闩、四类资源租约、换槽和重启恢复，主要覆盖验收 2、5。
4. 摊青失水及嫩度采集账簿对应覆盖矩阵、嫩度守恒、定点算术和探头/水分仪尝试，主要覆盖验收 4、5。
5. 农残复判及终局仲裁器对应封存揭盲、板孔读数、农残边界、复判版本链、独立复核和杀青凭据，主要覆盖验收 5、6、7。
6. Go HTTP API对应稳定错误码、原因排序、JSON 契约、事务映射、Docker 运行入口和公共测试夹具，横向覆盖全部验收项并直接支撑验收 8。

## 独特性

本计划独特地把名优绿茶鲜叶进入杀青机前的摊青时点覆盖、嫩度分类守恒、三联盲样农残快筛和理化复测统一在同一批次聚合中，并以茶筐封签一次性门闩、多类加工资源租约、不可覆盖复判代次链及终局单写屏障形成可重启的联合检验闭环；其对象、证据矩阵和放行条件均专属于绿茶杀青前工艺，不复用此前农产品入厂、转场或放播项目的标题与流程表达。
